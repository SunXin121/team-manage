# 应用配置
APP_NAME="GPT Team 管理系统"
APP_VERSION="0.1.0"
APP_HOST="0.0.0.0"
APP_PORT=8008
DEBUG=True

# 数据库配置
DATABASE_URL="sqlite+aiosqlite:///./team_manage.db"

# 安全配置
SECRET_KEY="your-secret-key-here-change-in-production"
ADMIN_PASSWORD="admin123"  # 首次启动后请修改

# 日志配置
LOG_LEVEL="INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# 代理配置 (可选)
PROXY=""  # 格式: http://host:port 或 socks5://host:port 或 socks5://user:pass@host:port
PROXY_ENABLED=False

# JWT 配置
JWT_VERIFY_SIGNATURE=False  # 开发环境可设为 False,生产环境建议设为 True

# 时区配置
TIMEZONE=Asia/Shanghai  # 默认使用中国时区

# Team 自动同步配置
TEAM_AUTO_SYNC_ENABLED=True  # 是否开启 Team 状态自动同步
TEAM_AUTO_SYNC_MIN_MINUTES=5  # 自动同步最小间隔（分钟）
TEAM_AUTO_SYNC_MAX_MINUTES=10  # 自动同步最大间隔（分钟）

# 到期成员清理任务配置
EXPIRED_MEMBER_CLEANUP_ENABLED=True  # 是否开启“每日凌晨到期成员清理”任务
EXPIRED_MEMBER_CLEANUP_DAYS=30  # 多少天视为到期

# 码支付配置
MAPAY_ID=""  # 码支付商户ID (pid)
MAPAY_KEY=""  # 码支付通信密钥
MAPAY_URL=""  # 码支付API地址
MAPAY_PRICE=10  # 支付金额 (元)
MAPAY_PRODUCT_NAME="GPT Team 会员"  # 商品名称
